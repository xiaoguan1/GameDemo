# GameDemo
	1.skynet引擎：git@github.com:cloudwu/skynet.git

	2.protobuf2版本协议：git@github.com:cloudwu/pbc.git
		特点：节点内共享 protobuf的pb，并且支持热更新（删减、修改、新增协议）
		但目前，还没做相关热更协议的安全处理。例如 协议有变动时 正好有玩家新下发协议等等？？

	3.websocket做网络的通信链接

	4.热更新：基于skynet.codecache 和 loadfile、dofile实现，代码文件是import.lua
		注释:
			skynet/3rd/lua/lauxlib.c 添加了clearone接口，提供清空某个单独文件的加载缓存

	5.目前打算使用mongodb做数据库


	6.集群设计：网关和玩家服务在一个节点,而其他玩法服务单独占用一个节点。若一些不热门的服务可以共用一个节点。提供基础功能的服务每个节点都必须启动，例如数据库服务、定时器服务等等。
		


	7.全局的临时共享数据，global_data



实现跨节点通信功能细节
	消息种类：异步（无需等待接收方的响应）、同步（需要等待接收方的响应）
	消息数据中必须附带 主动发送方 的节点信息(即"ip:port") 以及 服务地址(string or number)。
	消息类型整合：rpc协议类型占用1个字节、服务的协议类型占用3个字节

	异步消息：无需附带session，但还是带上节点信息和服务地址，虽然不带上感觉也行，但为了完整性或者方便后续追查但是带上吧，并且也就固定几个字节不存在浪费的可能。

	同步消息：必须附带session，因为需要对方的响应数据。

	超时限制：3秒

	心跳机制：
		间隔5秒进行一次发送。若在5秒内有消息发送出去，则该5秒内不需要发送心跳了。

		按节点类型来区分谁向谁发送心跳消息。目前是游戏服(即main_node结点) 向 其他节点发送心跳消息。而跨服节点向中心服节点发送心跳消息。被发送方 判断保活机制是 9*5 秒没有任何消息来则直接关闭socket。
		注释：因为socket的keepalive过于慢，不做保活机制有可能会形成死链而造成资源来不及释放的情况。故必须做保活机制。此外，如果有了保活机制关闭keepalive可以吗？看着是可以的，但还是维持skynet引擎的默认开启keepalive机制~~~






目录结构：
	

























